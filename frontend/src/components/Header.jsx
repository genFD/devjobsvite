import { Navbar, Search } from '../components';

const Header = () => {
  return (
    <header className="main-header">
      <Navbar />
      <Search />
    </header>
  );
};

export default Header;
